// Export an object containing constants
module.exports = {
    // Secret key used for authentication (e.g., with JSON Web Tokens)
    SECRET: "poofo123secretclave456",

    // Admin's email address (used for admin setup or configuration)
    ADMIN_EMAIL: "sebastianrincon834@gmail.com",

    // Admin's username (used for admin setup or configuration)
    ADMIN_USERNAME: "SebastianB",

    // Admin's password (used for admin setup or configuration)
    ADMIN_PASSWORD: "admin123"
};
